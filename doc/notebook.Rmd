---
title: "R Notebook"
output: html_notebook
---

```{r}
rm(list = ls())
library(tidyverse)
library(ggplot2)
library(lmtest)
```

```{r}
df = read.table("../data/houseprices.txt", header = TRUE, sep = "\t")
df$Fireplace <- as.factor(df$Fireplace)
summary(df)

```

```{r fig.height=10, fig.width=15}
plot(df$Size, df$Price)

plot(df$Baths, df$Price)
plot(df$Bedrooms, df$Price)
plot(df$Fireplace, df$Price)
plot(df$Acres, df$Price)
plot(df$Age, df$Price)
```

```{r}
df.full_lm <- lm(Price ~ ., data = df)
df.full_res = resid(df.full_lm)
summary(df.full_lm)



                # the horizon
```

```{r}
# log of response
data.full_loglm = 
```

```{r}
# whites test
bptest(df.full_lm, ~ disp*hp*am + I(disp^2) + I(hp^2)+ I(am^2), data = mtcars)
```
